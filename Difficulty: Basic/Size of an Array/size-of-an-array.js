class Solution {
    getSize(arr) {
        // code here
        return arr.length;
        // let count = 0;
        // while(arr[count]) {
        //     count++;
        // }
        // return count;
    }
}
